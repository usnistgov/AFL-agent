name: "Publish Documentation to NIST Pages"

on: [push, pull_request, delete]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: usnistgov/Docs4NIST@latest
        with:
          docs-folder: docs/
          formats: |-
            epub
            pdf
          pip-requirements: "requirements.txt"

